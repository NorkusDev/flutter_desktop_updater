# desktop_updater 2.0 Roadmap

## Current State

desktop_updater 1.x is the legacy folder-based updater line. desktop_updater 2.x is the stable zip-first release line.

## Why Zip-First

Zip-first releases avoid public folder listing, work with signed URLs and private buckets, and make each update artifact easy to verify before extraction.

The rewrite exists because the 1.x folder contract made production hosting depend on public directory-style layouts. A 2.0 client only needs exact URLs for metadata and one artifact, so publishers can use S3/R2/GCS private buckets, signed URLs, reverse proxies, or CDNs without exposing listable folders.

## Release Contract

The app checks `app-archive.json`, the selected item points to `release.json`, and `release.json` points to one zip artifact with length, SHA-256, and production authenticity metadata.

The 2.0 `app-archive.json` shape keeps `items` for continuity, but schema version 3 items must use `release` instead of the legacy folder `url`. `buildNumber` is optional in schema version 3 metadata; missing build numbers fall back to semantic version ordering so buildless installed apps are not forced into false-positive updates.

## Release Notes Capability

Release notes are planned as an optional runtime capability, not as part of the
core download/install state. The runtime should support a descriptor-aware
`releaseNotesLoader`, a convenience `releaseNotesUrl`, public release notes
models, and a separate release notes state so custom UI can render inline
panels, side sheets, or changelog pages. The stock `UpdateCard` can consume the
same controller API for a ready-made bottom sheet.

The first runtime slice should also accept the simple contributor-friendly
`{ "data": [{ "type": "feat", "message": "..." }] }` JSON shape. A later CLI
slice may add an optional `releaseNotes` reference to `release.json` and teach
`release publish` / `release validate` to produce and validate versioned
`release-notes.json` files.

## Platform Behavior

- macOS replaces a complete `.app` bundle created and extracted with `/usr/bin/ditto`.
- Windows replaces a complete app directory after the running process exits.
- Linux uses one documented strategy for 2.0 final: directory bundle zip unless AppImage support is explicitly selected before release.

## macOS Package Manager Compatibility

Swift Package Manager is the primary macOS plugin integration path for 2.0. The package still keeps the CocoaPods podspec as the fallback compatibility path for apps that explicitly disable SwiftPM or run older Flutter tooling. SwiftPM checks must pass without CocoaPods; CocoaPods checks are a separate fallback gate.

## Private Bucket Guidance

Do not rely on bucket listing. Publish exact descriptor and artifact URLs. Signed URLs, private proxies, and CDN URLs are supported as long as clients can fetch the exact URL before expiry.

## Release Gates

2.0 final requires unit, widget, native, and e2e tests for check, download, verify, stage, install scheduling, rollback, and migration examples.

macOS integration can be run locally on a macOS host:

- SwiftPM-first: enable SwiftPM and run the example app/plugin tests without requiring `pod install`.
- CocoaPods fallback: disable SwiftPM, then build or test the example app to preserve compatibility.
- Production-trusted acceptance: build an exported Release `.app`, sign it with a `Developer ID Application` identity from the same Team ID as the installed app, enable hardened runtime, notarize it, staple the ticket, package it with `ditto`, and verify the update through the native production gates.
- Unsigned macOS Release mechanics: app owners may set `allowUnsignedMacOSUpdates: true` to bypass codesign, Gatekeeper, stapler, and Team ID gates. This is an explicit owner opt-out for internal or user-controlled distribution, not a production-trusted path. The helper still requires a complete `.app` with the same bundle identifier.
- App Sandbox must stay disabled for the whole-app replacement strategy, and production entitlements must not include `get-task-allow`. A sandboxed Mac App Store style app should use the store update channel instead of this self-updater.

Windows and Linux integration are covered by hosted GitHub Actions runners.

## Production Readiness Definitions

- Debug update ready: the platform can run integration tests and the native smoke runner against debug output.
- Release mechanics ready: the platform can build Release output and run the same update smoke against Release output.
- Production-trusted ready: the Release artifact also satisfies the platform's publisher-authenticity gate.

Unsigned Windows and Linux Release builds can be accepted as release-mechanics ready for package validation. They are not production-trusted unless the app adds the relevant publisher-authenticity layer. macOS is production-trusted only when Developer ID signing, hardened runtime, notarization, stapling, Gatekeeper, bundle ID, and Team ID gates pass; `allowUnsignedMacOSUpdates: true` keeps macOS in release-mechanics-only status.

Current acceptance status:

- macOS debug update: ready.
- macOS production-trusted native replacement: ready only after Developer ID signing, notarization, stapling, Gatekeeper assessment, production native smoke, and relaunch smoke.
- macOS unsigned Release mechanics: ready with the explicit `allowUnsignedMacOSUpdates` owner opt-out; not production-trusted.
- macOS hosted zip-first E2E: production-trusted ready after HTTPS hosting with Developer ID signing, notarization, stapling, Gatekeeper assessment, production native gates, and relaunch smoke evidence.
- Windows debug update: ready on GitHub Actions.
- Windows unsigned release mechanics: covered by GitHub Actions Release build, native tests, integration tests, and smoke.
- Windows production-trusted signed update: planned; requires Authenticode signing and `signtool` verification.
- Linux debug update: ready on GitHub Actions.
- Linux unsigned release mechanics: covered by GitHub Actions Release build, native tests, integration tests, and smoke.
- Linux direct-zip production-trusted update: planned; requires a descriptor authenticity gate or another pinned publisher signature policy.

## Compatibility

1.x installed apps should remain on the 1.x release contract until their publishers migrate their release pipeline and app code.
